#include <iostream>

using namespace std;

// nums with size n contains numbers from [0, n], find the missing number
// [3, 4, 1, 0, 5] -> missing is 2
int findMissingNumber(vector<int> &nums)
{
    
}