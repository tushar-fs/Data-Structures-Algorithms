/**
 * Problem: Find Missing Number (LC #268)
 * Given an array of size n containing numbers from [0, n], find the one
 * missing number. Example: [3, 4, 1, 0, 5] → missing is 2.
 * Time Complexity: O(n)
 * Space Complexity: O(1)
 */
#include <iostream>

using namespace std;
int findMissingNumber(vector<int> &nums)
{
    
}